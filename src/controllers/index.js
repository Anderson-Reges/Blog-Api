const loginControl = require('./login.controller');
const userControl = require('./user.controller');
const categoryControl = require('./category.controller');
const postControl = require('./post.controller');

module.exports = {
  loginControl,
  userControl,
  categoryControl,
  postControl,
};